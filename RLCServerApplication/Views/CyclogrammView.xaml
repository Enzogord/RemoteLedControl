<UserControl x:Class="RLCServerApplication.Views.CyclogrammView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:RLCServerApplication.ViewModels"
             xmlns:local="clr-namespace:RLCServerApplication.Views"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance vm:CyclogrammViewModel}"
             d:DesignHeight="100" d:DesignWidth="200">
    <Grid VerticalAlignment="Center">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="0" 
                        HorizontalAlignment="Stretch"
                        IsReadOnly="True"
                        Text="{Binding Cyclogramm.FilePath}"></TextBox>
            <Button 
                Grid.Column="1" 
                Margin="2, 0, 0, 0" 
                Padding="5, 0, 5, 0"
                Content="..."
                Command="{Binding SelectCyclogrammFile}"
                />
        </Grid>
    </Grid>
</UserControl>
