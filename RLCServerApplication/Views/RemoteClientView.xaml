<UserControl x:Class="RLCServerApplication.Views.RemoteClientView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RLCServerApplication.Views"
             xmlns:vm="clr-namespace:RLCServerApplication.ViewModels"
             xmlns:model="clr-namespace:RLCCore;assembly=Core"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance vm:RemoteClientViewModel}" d:DesignWidth="200" d:DesignHeight="150"
             >
    <UserControl.Resources>
        <DataTemplate x:Key="CyclogrammView" DataType="{x:Type vm:CyclogrammViewModel}">
            <local:CyclogrammView/>
        </DataTemplate>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>
            
            <TextBlock Grid.Column="0" Grid.Row="0" Margin="3" VerticalAlignment="Center" HorizontalAlignment="Right">Номер</TextBlock>
            <TextBox Grid.Column="1" Grid.Row="0" Margin="3" Text="{Binding Number}"></TextBox>

            <TextBlock Grid.Column="0" Grid.Row="1" Margin="3" VerticalAlignment="Center" HorizontalAlignment="Right">Имя</TextBlock>
            <TextBox Grid.Column="1" Grid.Row="1" Margin="3" Text="{Binding Name}"></TextBox>

            <TextBlock Grid.Column="0" Grid.Row="2" Margin="3" VerticalAlignment="Center" HorizontalAlignment="Right">Циклограмма</TextBlock>
            <ContentControl Grid.Column="1" Grid.Row="2" Margin="3"
                            Content="{Binding CyclogrammViewModel}"
                            ContentTemplate="{StaticResource CyclogrammView}"></ContentControl>
        </Grid>

        <Grid Grid.Row="1" HorizontalAlignment="Right">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Padding="10, 0, 10, 0" Margin="5, 0, 3, 0" 
                    Content="Ок" Command="{Binding SaveChangesCommand}"/>
            <Button Grid.Column="1" Padding="10, 0, 10, 0" Margin="0, 0, 3, 0"
                    Content="Отмена" Command="{Binding CloseCommand}"/>
        </Grid>
    </Grid>
</UserControl>
