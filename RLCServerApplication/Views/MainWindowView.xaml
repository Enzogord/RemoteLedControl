<Window x:Class="RLCServerApplication.Views.MainWindowView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RLCServerApplication.Views"
        xmlns:vm="clr-namespace:RLCServerApplication.ViewModels"
        mc:Ignorable="d"
        Title="MainWindowView" Height="450" Width="800"
        d:DataContext="{d:DesignInstance vm:MainWindowViewModel}"
        >

    <Window.Resources>
        <!--<local:SettingsView x:Key="Settings"/>-->
        <DataTemplate x:Key="SettingsTemplate" DataType="{x:Type vm:SettingsViewModel}">
            <local:SettingsView></local:SettingsView>
        </DataTemplate>

        <DataTemplate x:Key="RemoteClientsTemplate" DataType="{x:Type vm:RemoteClientsViewModel}">
            <local:RemoteClientsView></local:RemoteClientsView>
        </DataTemplate>
    </Window.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>        
        <Menu Grid.Row="0" Height="auto">
            <MenuItem Header="Файл">
                <MenuItem Header="Создать"></MenuItem>
            </MenuItem>
        </Menu>

        <Grid Grid.Row="1">
            <StackPanel Orientation="Horizontal">
                <Button Margin="5" Command="{Binding PlayCommand}">Play</Button>
                <Button Margin="5" Command="{Binding StopCommand}">Stop</Button>
                <Button Margin="5" Command="{Binding PauseCommand}">Pause</Button>
                <Button Margin="5" Command="{Binding StartServicesCommand}">Start server</Button>
                <Button Margin="5" Command="{Binding StopServicesCommand}">Stop server</Button>
                <!--<TextBlock Margin="5" Text="{Binding }">Stop server</TextBlock>-->
            </StackPanel>
        </Grid>
        
        <TabControl Grid.Row="2" TabStripPlacement="Left">
            <TabItem Header="Настройки" Padding="10">
                <ContentPresenter Content="{Binding SettingsViewModel}" ContentTemplate="{StaticResource SettingsTemplate}" />
            </TabItem>

            <TabItem Header="Клиенты" Padding="10">
                <ContentPresenter Content="{Binding RemoteClientsViewModel}" ContentTemplate="{StaticResource RemoteClientsTemplate}" />
            </TabItem>

        </TabControl>
        
        <!--<Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto"></ColumnDefinition>
                <ColumnDefinition Width="1*"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0" Orientation="Vertical">
                <Button Margin="5">Настройки</Button>
                <Button Margin="5">Клиенты</Button>
            </StackPanel>
            <ContentPresenter Grid.Column="1" ></ContentPresenter>
        </Grid>-->
        <!--<TextBlock Grid.Row="1" Text="{Binding AutoUpdatedMessage}"></TextBlock>
        <Button Grid.Row="2" Command="{Binding AutoFillMessageCommand}" Content="Старт"></Button>-->
    </Grid>
</Window>
